apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasky-app.fullname" . }}
  labels:
    {{- include "tasky-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tasky-app.selectorLabels" . | nindent 4 }}
